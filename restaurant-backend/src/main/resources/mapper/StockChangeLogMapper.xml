<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.restaurant.mapper.StockChangeLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO stock_change_log(dish_id, order_id, quantity, change_type, reason)
        VALUES(#{dishId}, #{orderId}, #{quantity}, #{changeType}, #{reason})
    </insert>
</mapper>
