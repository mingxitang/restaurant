<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.restaurant.mapper.TableInfoMapper">
    <select id="findAll" resultType="TableInfo">
        SELECT * FROM table_info
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY table_number
    </select>

    <select id="findById" resultType="TableInfo">
        SELECT * FROM table_info WHERE table_id = #{tableId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="tableId">
        INSERT INTO table_info(table_number, area, capacity, status)
        VALUES(#{tableNumber}, #{area}, #{capacity}, #{status})
    </insert>

    <update id="update">
        UPDATE table_info
        <set>
            <if test="tableNumber != null">table_number = #{tableNumber},</if>
            <if test="area != null">area = #{area},</if>
            <if test="capacity != null">capacity = #{capacity},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE table_id = #{tableId}
    </update>

    <update id="updateStatus">
        UPDATE table_info SET status = #{status} WHERE table_id = #{tableId}
    </update>

    <delete id="delete">
        DELETE FROM table_info WHERE table_id = #{tableId}
    </delete>
</mapper>
